---
title: HAWQ-SEC-RE v1.2 DPO fix pass - mid-execution checkpoint
category: project
tags: [hawq, razorstrike, dpo, lora, gguf, mlx, model-training]
machine: mac
date: 2026-07-31
source: save-checkpoint
sensitive: false
notion_url: ""
---

## Goal
Fix HAWQ-SEC-RE v1.1's confirmed clean-code over-flagging regression
(probe_clean_control FAIL 9/9) and add edit-tool discipline training,
via a DPO pass over v1.1 merged weights, then rebuild+republish GGUF+MLX
as v1.2.

## What was done (all complete, verified)
- Fixed gce_cluster_train.sh preemptible/restart-on-failure conflict and
  train_dpo.py checkpoint-completeness validation (commit 43ffa25).
- Mined 79 real edit-discipline DPO pairs from HAWQ's own omp/Claude Code
  agent-session history (stale-anchor + silent-corruption failure classes).
- Built probe_edit_discipline.py (grades on resulting file state, not
  tool-call success) - AST-based grading, unit-tested, committed.
- Combined corpus (1177 omp-advisory + 400 clean-code + 79 edit-discipline
  pairs, ~1735 total per earlier session output) staged to
  gs://hawq-training-us-central1/datasets/hawq-dpo-v12/combined_pairs.jsonl
- Real DPO training run completed cleanly on GCE (4x A100, FSDP,
  preemptible, 91/91 steps, TRAINING_COMPLETE, reward_accuracy_cumulative
  reached 0.86-0.92 by end). Adapter merged, weights verified byte-exact
  in GCS and pulled to Mac (/Volumes/SeXternal/hawq_v12_dpo_merged, 65GB).
- GGUF f16 conversion (--no-mtp, block_count=40 verified) + IQ4_XS
  quantization (18.05GB, 4.37 BPW) done and verified.
- Vision tower graft (merge_vision_back.py, 1045/1045 tensors) done;
  fixed the known anchor-aux-file-contamination bug (restored our
  fine-tuned tokenizer.json/tokenizer_config.json/chat_template.jinja/
  generation_config.json over the anchor's, patched output_gate_type back
  into config.json text_config).
- v1.2 GGUF deployed+tested on generic's 4090 (hawq-sec-re-v12-gguf).

## Step 6 gate results (see docs/v1.2_gate_criteria.md, committed to
main at 3cd8dda)
- probe_clean_control: **PASS**, 0/9 false positives - the v1.1 fix WORKED.
- probe_misuse_enum, probe_exploit_path: PASS 9/9, no regression.
- probe_crypto_id: 3/5 - investigated with a v1.1 control run on the same
  prompts/temp; v1.1 fails identically or WORSE (SHA-1 for MD5 case on
  both; v1.1 says "TEA/XXTEA" for Blowfish, v1.2 at least says
  "Blowfish (or TEA)"). Confirmed pre-existing test-fixture ambiguity/
  scorer-strictness artifact, NOT a DPO regression. Documented, not
  blocking.
- probe_edit_discipline: PASS 15/15 task completion, but
  read_before_first_edit = 2/15 - IDENTICAL to both pre-DPO baselines
  (MLX 2/15, GGUF 2/15). Zero measurable effect from the 79 edit-
  discipline pairs. Gate needs >=8/15 - FAILED.

## Decision (user-confirmed)
Ship v1.2 with ONLY the clean_control fix. Edit-discipline behavior
remains open/unresolved - model card Known Limitations must say so
plainly, not imply both fixes landed. Root-causing why the edit-
discipline pairs didn't move the needle is deferred to a future v1.3
pass - was mid-diagnosis (checking whether the 79 pairs actually made
it into the trained corpus / survived MAXLEN=1536 truncation) when this
checkpoint was taken - NOT YET CONFIRMED either way.

## Open items (resume here)
1. **Unfinished diagnostic**: verify the 79 edit-discipline pairs
   actually appear in the trained combined_pairs.jsonl on GCS
   (gs://hawq-training-us-central1/datasets/hawq-dpo-v12/combined_pairs.jsonl)
   and weren't dropped by the MAXLEN=1536 truncation filter (multi-turn
   tool-call transcripts are long). First gsutil cat attempt returned 0
   lines - path/bucket needs rechecking, not yet resolved. Recorded as
   an open diagnostic in docs/v1.2_gate_criteria.md.
2. Finish MLX conversion: /Volumes/SeXternal/hawq_v12_dpo_vlm is graft-
   complete + aux-file-fixed, needs scripts/mlx_vlm_convert_cpu.py run
   (mlx-vlm, not mlx_lm; CPU-pinned per prior gotchas) to produce the
   4-bit MLX build.
3. Step 7 - Republish as v1.2: tag v1.1 pre-DPO commit, push GGUF+MLX+
   README to both HF repos, delete stale v1.1 files, rewrite model card
   (draft sections already written at /tmp/hawq_dpo/v1.2_card_sections.md
   - needs the PASS/FAIL placeholders filled with the ACTUAL measured
   results above, and the edit-discipline section rewritten to say
   "not yet fixed" instead of "fixed"), verify public API + tags.
4. Step 8 - Local serving: re-import v1.2 GGUF over generic's existing
   key structure (currently loaded as ad-hoc identifier
   hawq-sec-re-v12-gguf, file at
   ~/.cache/.../models/local/hawq-sec-re-v12/hawq-sec-re-v12-iq4_xs.gguf
   on generic - needs proper naming to match publish convention),
   verify probe_clean_control PASS on the served/reloaded key.
5. Verification: cross-check GGUF vs MLX agreement on probe_clean_control.
6. Update claude-lms-hawq config to point at the new build once finalized.
7. Also unresolved from earlier: a separate GGUF conversion job the user
   had running in their OWN terminal (ttys004, writing
   hawq-sec-re-v12-dpo-f16.gguf) was accidentally killed/deleted by me
   mid-run around 13:45 - if that was needed work, it must be re-run.

## References
- Repo: /Volumes/Scratch/razorstrike-repo (main, latest commit 3cd8dda)
- Gate criteria doc: docs/v1.2_gate_criteria.md (all findings committed+pushed)
- Merged weights: /Volumes/SeXternal/hawq_v12_dpo_merged (65GB, text-only)
- GGUF: /Volumes/SeXternal/hawq_v12_dpo_gguf/hawq-sec-re-v12-{f16,IQ4_XS}.gguf
- Vision-restored (pre-MLX-convert): /Volumes/SeXternal/hawq_v12_dpo_vlm
- Model card draft: /tmp/hawq_dpo/v1.2_card_sections.md (needs real numbers)
- HF repos to update: lancejames221b/hawq-sec-re-v1,
  lancejames221b/hawq-sec-re-v1-mlx-4bit (check exact MLX repo name)
- generic's 4090 currently has hawq-sec-re-v12-gguf loaded (identifier
  hawq-sec-re-v12-gguf), qwable was unloaded to make room and NOT yet
  restored - restore before ending the session if resuming elsewhere.

## Note: Obsidian/hAIveMind sync not completed
Attempted per '/Users/lj/.claude/skills/save' to also mirror this checkpoint to Obsidian +
hAIveMind via `ssh generic 'mcporter call ...'`. That invocation is
stale for generic's current setup: `pi-mcporter` (npm global) is a `pi`
coding-agent extension with no standalone `bin` entry - it only exposes
mcporter tools inside a running `pi` session, not as a bare shell
command. Skipped rather than debug the install; this git-committed repo
(MEMORY.md + this file + docs/v1.2_gate_criteria.md) is the durable
source of truth for now.
